import HEADER_ACTION_TYPES from './header.types';

export const onLoginPage = (header) => ({
    type: HEADER_ACTION_TYPES.ON_LOGIN_PAGE
});

export const onHomePage = (header) => ({
    type: HEADER_ACTION_TYPES.ON_HOME_PAGE
});